var React = require('react');

var Home = React.createClass({
  render: function() {
    return (
      <h1 className="text-center">
      What's the Weather?
      </h1>
    );
  }
});

module.exports = Home;
